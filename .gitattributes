# 强制所有文本文件在提交时统一为 LF
* text=auto

*.txt text eol=lf

# Dockerfile 和 .dockerignore 使用 LF
Dockerfile text eol=lf
.dockerignore text eol=lf

# CMakeLists.txt 构建文件使用 LF
**/CMakeLists.txt text eol=lf

# Shell 脚本也强制 LF
*.sh text eol=lf